---
# ansible-galaxy collection install -r requirements.yml
collections:
  - name: pfsensible.core      # pfSense CE config over SSH (vlan/interface/rule/alias/nat/gateway/...)
    version: ">=0.7.1"
  - name: community.general    # sysrc (FreeBSD host role)
